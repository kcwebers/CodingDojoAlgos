// Given an array comprised ONLY of numbers, find the Mean, Median and Mode values
// You can either print these values to the console or returnt he values!

function mean(arr) { // the meand == the average of the values in the array
    // your code here
}

function median(arr) { // the mdeian == the center value of a SORTED array (the trick here is your array isn't sorted yet!)
    // your code here
}

function mode(arr) { // the mode == the most common value in an array
    // your code here
}



// Given two arrays that are sorted but not necessarily the same length, find the median value. If the number of values is even, return the average of the two middle values.
// Example #1) If given ([1,5,9], [1,2,3,4,5,6]), the returned value should be 4. 
// Example #2) If given ([1,5,9], [1,2,3,4,5]), the returned value should be 3.5. 

function medianOfTwo(arr1, arr2) {
    
}